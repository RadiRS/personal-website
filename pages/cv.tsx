import React from 'react';
import BaseLayout from '../components/layouts/base-layout';

type Props = {};

const CV = (props: Props) => {
  return (
    <BaseLayout>
      <h1>CV</h1>
    </BaseLayout>
  );
};

export default CV;
